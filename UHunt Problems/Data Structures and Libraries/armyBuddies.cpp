#include<iostream>
#include<algorithm>
#include<vector>
#include<numeric>
using namespace std;

int main() {
	int s, b;
	ios::sync_with_stdio(false);
	while (cin >> s >> b && s != 0 && b != 0) {
		while (b--) {
			int l,r;
			cin >> l >> r;
		}
	}
	return 0;
}
